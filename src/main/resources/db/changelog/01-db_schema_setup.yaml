databaseChangeLog:
  - changeSet:
      id: createSchemaAndUser
      author: sujith
      changes:
        - sql:
            dbms: postgresql
            sql: |
              CREATE SCHEMA IF NOT EXISTS moviehall;
              CREATE USER root WITH PASSWORD 'root';
              GRANT ALL PRIVILEGES ON SCHEMA moviehall TO root;